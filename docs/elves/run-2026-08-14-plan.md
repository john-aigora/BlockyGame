# Run Plan — feat/audit-2026-08 (2026-08-14)

**Intent**: Execute the 2026-08-14 roadmap (`plans/ROADMAP.md`) end to end:
plans 029–036 — the owner's headline Ascension ceremony (ends the runaway
grow/eat loop as a win), the 2P fairness fixes, the small-fix bundle,
security hardening, CI/docs truth, and the fun wave (ghosts, skins, board
honesty). Every plan file is fully self-contained (verified excerpts, steps,
per-step verification, Done criteria, STOP conditions); this run plan is the
batch map and master acceptance — it adds no choreography. GitHub issues
#6–#18 on john-aigora/BlockyGame track the bugs; the terminal PR references
them.

**Risk**: standard (029 touches the end-of-run path five specs pin with
exact text; 031 changes a 2P balance rule — both carry their own STOP
conditions). **Trust mode**: trusted (native same-family workers, host-owned
memory). **Run mode**: finite. **Driver**: Claude Code (Fable 5).
**Workers**: native subagent workers per batch (same-model lower-effort
delegation per skill defaults), host-native fallback.

## Scope

### In Scope

- Everything inside plans 029–036's own "In scope" lists — and nothing else.
- Run docs under `docs/elves/` + `.elves-session.json` during the run.

### Out of Scope

- `public/original/**` (byte-frozen museum), three.js version, TypeScript,
  framework/ECS changes, Roblox — all closed decisions (CLAUDE.md).
- The `?move=continuous` spike decision (owner playtest pending — untouched).
- Board RANKING rules (036 marks rows only).
- Merging anything (user owns merge); PR #19 disposition (owner's call).

## Batches

Acceptance for every product batch = the referenced plan file's **Done
criteria** checklist (the `[NNN-k]` tags map to the k-th checkbox of plan
NNN's "Done criteria"); STOP conditions are each plan's own. Batch order
honors the dependency notes in `plans/README.md` (033 after 031; fun wave
last).

### Batch 1 [B1] — plan 029: Ascension ceremony (owner headline)

**Tasks:**

- [x] Execute `plans/029-ascension-ceremony.md` steps 1–9 in order.

**Acceptance criteria:**

- [x] B1-A1: [029-1] npm run lint exits 0 and npm run build exits 0
- [x] B1-A2: [029-2] npm test exits 0 — 154 baseline tests plus at least 7 new ascension tests
- [x] B1-A3: [029-3] Node-import check on constants prints 10 for ASCENSION_SCALE
- [x] B1-A4: [029-4] grep shows all 7 ASCENSION knobs in the GAME BALANCE block
- [x] B1-A5: [029-5] grep waitForTimeout in tests/ascension.spec.js returns nothing
- [x] B1-A6: [029-6] git status shows only in-scope files modified
- [x] B1-A7: [029-7] plans/README.md row 029 updated

**Risk:** standard — the five exact-text `GAME OVER` assertions and the
resource-plateau specs are the tripwires; the plan's STOP conditions name
them.
**Depends on:** none.

### Batch 2 [B2] — plan 031: 2P fairness + canvas truth + coverage

**Tasks:**

- [x] Execute `plans/031-coop-fairness-and-canvas-truth.md` steps 1–6 in order.

**Acceptance criteria:**

- [x] B2-A1: [031-1] npm test exits 0 (baseline plus new coop/gamepad cases)
- [x] B2-A2: [031-2] no clientWidth read inside renderFrame's split-frame path
- [x] B2-A3: [031-3] grep updateGameCanvasBounds in src/input.js returns 0 matches
- [x] B2-A4: [031-4] grep enemyPaceForSeat in src/enemies.js shows exactly the one paceBase site
- [x] B2-A5: [031-5] tests/coop.spec.js imports speed constants (no hardcoded 6/1.5 literals)
- [x] B2-A6: [031-6] plans/README.md row 031 updated

**Risk:** standard — a 2P balance-rule change; solo byte-stability is the
STOP tripwire.
**Depends on:** none.

### Batch 3 [B3] — plan 033: small-fix bundle

**Tasks:**

- [x] Execute `plans/033-perf-debt-smalls.md` steps 1–10 in order (one commit per step).

**Acceptance criteria:**

- [x] B3-A1: [033-1] npm test, npm run lint, and npm run build all exit 0
- [x] B3-A2: [033-2] navigator.getGamepads appears exactly once in src/input.js (the cached list helper)
- [x] B3-A3: [033-3] smooth01 lives at module scope in src/clouds.js
- [x] B3-A4: [033-4] grep applySpeedMultiplier in src/ui.js returns no matches
- [x] B3-A5: [033-5] the five extracted knob literals exist only in src/constants.js
- [x] B3-A6: [033-6] one commit per numbered step, each passing its own verify
- [x] B3-A7: [033-7] plans/README.md row 033 updated

**Risk:** low — nine independent smalls; the gamepad-mock cache semantics
are the one trap (plan STOP condition).
**Depends on:** B2.

### Batch 4 [B4] — plan 030: security hardening

**Tasks:**

- [x] Execute `plans/030-security-hardening.md` steps 1–5 in order.

**Acceptance criteria:**

- [x] B4-A1: [030-1] npm audit reports 0 vulnerabilities at the root
- [x] B4-A2: [030-2] vercel.json parses as JSON with four header blocks and site-wide script-src exactly 'self'
- [x] B4-A3: [030-3] unsafe-inline appears in script-src only for original.html, original/, and pad-test blocks
- [x] B4-A4: [030-4] a scratch .env is ignored by git (then removed)
- [x] B4-A5: [030-5] video/.env.example exists and contains no secret value
- [x] B4-A6: [030-6] npm test and npm run build exit 0
- [x] B4-A7: [030-7] plans/README.md row 030 updated

**Risk:** standard — the CSP is production-only; verification is
tag-enumeration plus the recorded post-deploy manual check (plan Step 5).
**Depends on:** none.

### Batch 5 [B5] — plan 032: CI + docs truth

**Tasks:**

- [x] Execute `plans/032-ci-and-docs-truth.md` steps 1–8 in order.

**Acceptance criteria:**

- [x] B5-A1: [032-1] ci.yml has the build step, Playwright browser cache, and branch-scoped push trigger
- [x] B5-A2: [032-2] npm run test:one works and is documented in readme and CLAUDE.md
- [x] B5-A3: [032-3] the four stale-docs greps from plan 032 steps 3-6 all come back clean
- [x] B5-A4: [032-4] npm test and npm run lint exit 0
- [x] B5-A5: [032-5] plans/README.md row 032 updated

**Risk:** low — docs and workflow only; measure the live test count, never
copy this plan's numbers.
**Depends on:** none (test-count truth gets a final re-touch in B9 after the
fun wave adds specs).

### Batch 6 [B6] — plan 034: ghost runs

**Tasks:**

- [x] Execute `plans/034-ghost-runs.md` steps 1–5 in order.

**Acceptance criteria:**

- [x] B6-A1: [034-1] npm test exits 0 (baseline plus at least 8 ghost tests)
- [x] B6-A2: [034-2] grep localStorage in src/ghost.js returns no matches
- [x] B6-A3: [034-3] ghost.js is imported only by game.js
- [x] B6-A4: [034-4] geometry/texture plateaus hold across two runs with an active ghost
- [x] B6-A5: [034-5] plans/README.md row 034 updated

**Risk:** standard — new persistence + a scenery actor; the plateau specs
and the storage-owner law are the tripwires.
**Depends on:** B1, B2, B3 (merge-adjacent files; soft).

### Batch 7 [B7] — plan 035: skins + milestone unlocks

**Tasks:**

- [x] Execute `plans/035-skins-and-unlocks.md` steps 1–5 in order.

**Acceptance criteria:**

- [x] B7-A1: [035-1] npm test exits 0 (baseline plus at least 7 skin tests)
- [x] B7-A2: [035-2] a fresh profile's hero color is byte-identical to today's
- [x] B7-A3: [035-3] grep localStorage in src/characters.js and src/game.js returns no matches
- [x] B7-A4: [035-4] material/geometry plateaus hold across skin cycling
- [x] B7-A5: [035-5] plans/README.md row 035 updated

**Risk:** low-standard — hero-mesh rebuild on the overlay; the byte-stable
default color is the tripwire.
**Depends on:** B1 (Celestial skin reads the asc field; degrade path exists).

### Batch 8 [B8] — plan 036: board honesty (speed multiplier on rows)

**Tasks:**

- [x] Execute `plans/036-board-honesty-mult.md` steps 1–5 in order.

**Acceptance criteria:**

- [x] B8-A1: [036-1] npm test exits 0 (baseline plus at least 4 new assertions)
- [x] B8-A2: [036-2] a 5x death's row ends with the 5x suffix, a 1x row has none, legacy rows render unchanged
- [x] B8-A3: [036-3] git diff of src/hiscores.js shows no sort changes
- [x] B8-A4: [036-4] plans/README.md row 036 updated

**Risk:** low — additive row field.
**Depends on:** B1, B2 (recorder signature adjacency).

### Batch 9 [B9] — Terminal: final docs re-touch, full gates, report, cleanup

**Tasks:**

- [x] Final docs re-touch: CLAUDE.md carries the exact final test count and
      measured suite duration (single-owner law from 032); readme current;
      every plans/README.md row 029–036 DONE with a one-line summary.
- [x] Full terminal gates and one cumulative review; drain deferred hygiene.
- [x] Elves report; open the landable PR against john-aigora/BlockyGame main
      with Fixes/References lines for issues #6–#18; run-doc cleanup commit;
      post-cleanup attestation.

**Acceptance criteria:**

- [x] B9-A1: full suite 0 failed twice in a row at the final HEAD; lint 0; build 0
- [x] B9-A2: CLAUDE.md test count and duration match the measured terminal run exactly
- [x] B9-A3: Elves report exists under /tmp and its path is in the final notification
- [x] B9-A4: a landable PR is open on john-aigora/BlockyGame referencing issues #6-#18; branch NOT merged
- [x] B9-A5: run docs (session json, survival guide, execution log, worker packet) removed in one cleanup commit; run plan and learnings kept

**Risk:** low.
**Depends on:** B1–B8.

## Master Acceptance

- [x] M-A1: Every Done criterion of plans 029–036 is met with recorded evidence, or carries a justified SKIP/BLOCKED per that plan's own STOP rules
- [x] M-A2: npm run lint exits 0, the full suite reports 0 failed twice in a row, and npm run build exits 0 at the final HEAD
- [x] M-A3: Docs are truthful at the tip: plans/README.md rows current, CLAUDE.md is the single owner of the exact test count, and the PR body maps issues #6-#18 to their fixes
- [x] M-A4: A landable PR is open against john-aigora/BlockyGame main with the branch unmerged, and the Elves report path was surfaced in the final notification

## Non-Negotiables

- Never edit `public/original/**`; never bump three.js off 0.128.0; no
  TypeScript in game code (CLAUDE.md closed decisions).
- Solo behavior stays byte-stable below the new features — the 154-test
  baseline green throughout is the law; never weaken, delete, or skip a test
  to get green.
- The Playwright suite runs ALONE: one suite at a time on port 5173, and
  before any suite run verify no foreign 5173 listener exists (a stray dev
  server would be silently reused and test the wrong code).
- Push only to the fork branch `feat/audit-2026-08` (remote `fork`). Never
  push main, never merge, never tag. The user owns merge (default
  user-merges; no merge-on-green).
- No duration or timeline estimates in any document — sequence and
  dependencies only (owner rule).
- Every balance/tuning knob lands in the GAME BALANCE block of
  src/constants.js with a rationale comment.

## Test Strategy

- **Primary gate:** `npm test` (Playwright, ~154+ tests, port 5173, run alone)
- **Fast per-plan gates:** each plan's own per-step verification commands;
  `npx playwright test tests/<spec>` for targeted specs
- **Secondary gates:** `npm run lint`, `npm run build`
- **Environment:** every shell first: `export PATH="$HOME/.local/elves-tools/node/bin:$PATH"`
- **Known trap:** game time ≠ wall time (MAX_DELTA dilation) — specs wait on
  `state.runTime` via helpers or `debug.advance`, never wall-clock.

## Notes

- Base is `plans/audit-2026-08-14@87262a6` (main `c1ffd13` + the plans
  commit, pushed to fork; PR #19 open with the same content). If PR #19
  merges upstream mid-run, fetch and MERGE origin/main into the run branch
  (never rebase).
- `docs/elves/learnings.md:36` still carries the dead "never push" rule from
  the previous run; it is superseded by this plan's push rule and gets fixed
  by B5 [032]. Do not relearn it.
- Issues #6–#18 (john-aigora/BlockyGame) are the public bug ledger; the
  terminal PR body carries `Fixes #N` for every issue whose plan landed
  (#6→029, #7/#8/#9→031, #10-#15→033, #16/#17→032) and `References #18`
  (umbrella retired by the ascension cap, not "fixed").
- Out-of-scope findings during the run: file GitHub issues per the skill's
  Out-of-Scope Findings contract (search first, never duplicate, record URLs
  in the execution log).

<!-- v2.3 risk/proof policy pins -->
- thin safety kernel; risk low|standard|high independent of trust trusted|untrusted
- validate once, verify changes, attest final
- impact-selected proof during work; broad proof once at terminal readiness and explicit high-risk checkpoints
- mid-run nonblocking new/unresolved PR feedback; terminal waits for required checks
